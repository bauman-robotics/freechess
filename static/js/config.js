// static/js/config.js
// ============================================
// КОНФИГУРАЦИЯ ИГРЫ
// ============================================

const CONFIG = {
    // ============================================
    // МАРСЕЛЬСКИЕ ШАХМАТЫ
    // ============================================
    MARSEILLE: {
        // Запрет на движение одной фигурой дважды за turn
        // true - запрещено (классические Марсельские шахматы)
        // false - разрешено (можно ходить одной фигурой дважды)
        DISABLE_SAME_PIECE_TWICE: true,
        
        // Белые начинают с одного хода (классическое правило)
        // true - белые делают только 1 ход в начале
        // false - белые делают 2 хода как и все
        WHITE_STARTS_WITH_ONE_MOVE: true,
        
        // Потеря второго хода при объявлении шаха
        // true - игрок теряет второй ход если объявил шах первым ходом
        // false - игрок не теряет второй ход
        LOSE_SECOND_MOVE_ON_CHECK: true
    },
    
    // ============================================
    // ОБЩИЕ НАСТРОЙКИ
    // ============================================
    GENERAL: {
        // Максимальное количество ходов в истории
        MAX_HISTORY: 200,
        
        // Задержка перед автосохранением (мс)
        AUTOSAVE_DELAY: 1000
    }
};

// Делаем конфиг глобально доступным
window.CONFIG = CONFIG;

console.log('⚙️ Конфигурация загружена:', CONFIG);